import colors from './colors';

const inputTheme = {
  colors: {
    primary: colors.Accent,
    placeholder: colors.Shade2,
    error: colors.Error,
  },
  roundness: 8,
};

export default inputTheme;
